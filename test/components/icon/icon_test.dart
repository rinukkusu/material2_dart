@TestOn('browser')
import 'package:test/test.dart';

void main() {
  test('icon test', () async {},
      skip:
          'Waiting for http and MockBackend module are ported to the Dart version to test the icon component.');
}
