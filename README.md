# material2_dart

A porting work from angular/material2 to Dart version.

This is a personal project to learn how to make material UI component with ng2.

## Test

    // Run test server.
    pub serve
    // Run tests on dartium.
    pub run test --pub-serve=8080 -p dartium
